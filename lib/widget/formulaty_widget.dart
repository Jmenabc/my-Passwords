import 'package:flutter/material.dart';

class FormularyWidget extends StatelessWidget {
  const FormularyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children:  [
          TextFormField(

          )
        ],
      ),
    );
  }
}
